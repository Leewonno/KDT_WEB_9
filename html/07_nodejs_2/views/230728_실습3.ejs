<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>실습3 구구단</title>
</head>
<body>
    <style>
        div{
            display: flex;
        }
    </style>

    <%=gugu%>
    <div>
        <% for(var i = 0; i<gugu.length; i++){ %>
            <ul>
            <% for(var j = 1; j<10; j++){ %>
                <li><%= gugu[i] %> x <%= j %> = <%= (gugu[i] * j) %></li>
            <% } %>
            <br />
            </ul>
        <% } %>
    </div>
</body>
</html>